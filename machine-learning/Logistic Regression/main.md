# 逻辑回归（Logistic Regression）
## 概念
逻辑回归是一种二分类算法，用于预测一个事件发生或不发生的概率。

## 原理
逻辑回归的原理基于逻辑函数（或逻辑函数，也称为Sigmoid函数）。
### sigmoid函数
sigmoid函数是一个S形函数，它的输出范围在0到1之间。
sigmoid函数的公式为：
$$
\sigma(x) = \frac{1}{1 + e^{-x}}
$$
其中，$x$是输入值，$e$是自然对数的底数（约2.71828）。
sigmoid